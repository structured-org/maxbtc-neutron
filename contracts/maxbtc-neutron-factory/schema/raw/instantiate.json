{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InstantiateMsg",
  "description": "InstantiateMsg configures the contract on initialization.",
  "type": "object",
  "required": [
    "binance_aum_contract",
    "ceffu_backend",
    "code_ids",
    "deposit_cost",
    "deposit_decimals",
    "deposit_denom",
    "deposit_forwarder_contract",
    "fee_collector_params",
    "maxbtc_denom",
    "operator",
    "owner",
    "salt",
    "waitosaur_observer_unlocker"
  ],
  "properties": {
    "binance_aum_contract": {
      "description": "Address of the binance AUM contract",
      "type": "string"
    },
    "ceffu_backend": {
      "type": "string"
    },
    "code_ids": {
      "$ref": "#/definitions/CodeIds"
    },
    "deposit_cost": {
      "description": "One-off cost (Decimal) charged when a user deposits to mint maxBTC",
      "allOf": [
        {
          "$ref": "#/definitions/Decimal"
        }
      ]
    },
    "deposit_decimals": {
      "description": "Number of decimals carried by the `deposit_denom` asset",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "deposit_denom": {
      "description": "Denom for user deposits (e.g. IBC-transferred BTC)",
      "type": "string"
    },
    "deposit_forwarder_contract": {
      "description": "Address of the deposit forwarder contract",
      "type": "string"
    },
    "deposits_cap": {
      "description": "Upper limit on total AUM; deposits are rejected once the cap (if present) is exceeded",
      "anyOf": [
        {
          "$ref": "#/definitions/Uint128"
        },
        {
          "type": "null"
        }
      ]
    },
    "fee_collector_params": {
      "description": "Instantiation parameters for the fee collector.",
      "allOf": [
        {
          "$ref": "#/definitions/FeeMinterParams"
        }
      ]
    },
    "maxbtc_denom": {
      "description": "Token factory sub-denom",
      "type": "string"
    },
    "operator": {
      "type": "string"
    },
    "owner": {
      "type": "string"
    },
    "salt": {
      "type": "string"
    },
    "waitosaur_observer_unlocker": {
      "description": "Address of the waitosaur observer unlocker",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "CodeIds": {
      "type": "object",
      "required": [
        "allowlist_contract_code_id",
        "core_code_id",
        "exchange_rate_provider_contract_code_id",
        "fee_collector_contract_code_id",
        "token_code_id",
        "waitosaur_holder_contract_code_id",
        "waitosaur_observer_contract_code_id",
        "withdrawal_manager_contract_code_id"
      ],
      "properties": {
        "allowlist_contract_code_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "core_code_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "exchange_rate_provider_contract_code_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "fee_collector_contract_code_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "token_code_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "waitosaur_holder_contract_code_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "waitosaur_observer_contract_code_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "withdrawal_manager_contract_code_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "Decimal": {
      "description": "A fixed-point decimal value with 18 fractional digits, i.e. Decimal(1_000_000_000_000_000_000) == 1.0\n\nThe greatest possible value that can be represented is 340282366920938463463.374607431768211455 (which is (2^128 - 1) / 10^18)",
      "type": "string"
    },
    "FeeMinterParams": {
      "description": "New struct to hold parameters for instantiating the fee collector contract.",
      "type": "object",
      "required": [
        "collection_period_seconds",
        "fee_apy_reduction_percentage"
      ],
      "properties": {
        "collection_period_seconds": {
          "description": "The duration in hours for each fee collection period.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "fee_apy_reduction_percentage": {
          "description": "The percentage of APY to be taken as a fee.",
          "allOf": [
            {
              "$ref": "#/definitions/Decimal"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "Uint128": {
      "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
      "type": "string"
    }
  }
}
